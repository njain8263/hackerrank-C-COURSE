#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i,sum=0;
    scanf("%d",&n);
    for(i=0;i<4;i++)
    {
     sum=sum+(n%10);
     n=n/10;
    }
    printf("%d",sum+n);
    return 0;
}
